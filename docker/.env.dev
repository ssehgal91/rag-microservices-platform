# --- Database ---
DB_HOST=db
DB_PORT=5432
DB_NAME=ragdb_dev
DB_USER=dev_user
DB_PASSWORD=dev_password

# --- API Keys ---
API_KEY=dev-api-key
JWT_SECRET=dev-jwt-secret

# --- Spring Profile ---
SPRING_PROFILES_ACTIVE=dev
